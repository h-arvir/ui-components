/** Tailwind v4 does not require this file for basic setup, but we can keep it for future customization. */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx,js,jsx}'],
  darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [],
}

